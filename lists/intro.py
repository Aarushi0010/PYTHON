# list items are ordered , changeable , allow duplicate values

list = ["apple" , "banana" , "orange"]
print(list)

# length of list
print(len(list))

# can contain different data types 
list = ["aarushi" , 22 , True]
print(type(list))

# we can use list() constructor to create a list
mylist = list (("water" , "food" , "bag"))
print(mylist)